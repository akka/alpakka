
akka {
  stream {
    alpakka {
      ibm {
        eventstore{
          # Hostname or IP of EventStore host machine
          host = ""
          # Portnumber
          port = 5555
          # Name of database
          database-name = ""
          # Table in database
          table-name = ""
          # Number of concurrent insert operations
          parallelism = 1
        }
      }
    }
  }
}
